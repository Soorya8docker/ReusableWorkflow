name: Call a reusable workflow

on:
  workflow_dispatch:
    inputs:
      Branch:
        description: 'Enter Sprint branch'
        type: string
        required: true

jobs:
  call-workflow:
    uses: Soorya8docker/AmexProject/.github/workflows/ReusableWorkflow.yml@main

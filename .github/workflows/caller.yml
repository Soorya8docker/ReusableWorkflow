name: Main Workflow

on:
  workflow_dispatch:
    inputs:
      Branch:
        description: 'Enter Sprint branch'
        type: string
        required: true

jobs:
  build:
    uses: Soorya8docker/AmexProject/.github/workflows/build.yml@main  # Replace with the reusable workflow repository path
    with:
      node-version: '16'
      run-tests: true
    secrets:
      NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
